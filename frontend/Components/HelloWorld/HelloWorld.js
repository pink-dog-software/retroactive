import React from 'react';
import styled from 'styled-components';

const StyledLabel = styled.label`
  font-size: 2rem;
  color: blue;
`;

const HelloWorld = () => {
  return <StyledLabel>Hello World</StyledLabel>;
};

export default HelloWorld;
